[project]
name = "agy-creative"
version = "0.1.0"
description = "MCP server: image generation and Emirati Arabic translation via the Antigravity CLI (agy)"
requires-python = ">=3.11"
dependencies = ["mcp[cli]>=2.2,<3"]

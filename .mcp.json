{
  "mcpServers": {
    "agy-creative": {
      "command": "uv",
      "args": [
        "run",
        "--quiet",
        "--project",
        "${CLAUDE_PLUGIN_ROOT}/server",
        "${CLAUDE_PLUGIN_ROOT}/server/server.py"
      ]
    }
  }
}
